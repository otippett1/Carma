# Workspace Rules

- Keep code mobile-first and accessible
- Use meaningful commit messages
- No secrets in repo; use .env and .env.example
- Run formatters/linters before PRs
- Keep API endpoints documented in /docs
